@model IEnumerable<OceanNex.Models.Predicao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TaxaPredicao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescricaoPredicao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Conta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TaxaPredicao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescricaoPredicao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Conta.Email)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PredicaoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PredicaoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PredicaoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
